services:
  db:
    image: postgres:15-alpine
    container_name: logic_app_db
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: AWDSAD1231235123
      POSTGRES_DB: logic_tasks_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
